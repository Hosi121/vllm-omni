# Jetson (unified memory): keep the decoder eager, small batches; budgets are
# absolute bytes derived at runtime, so gpu_memory_utilization is not used.
stages:
  - stage_id: 0
    max_num_seqs: 2
  - stage_id: 1
    max_num_seqs: 2
    enforce_eager: true
