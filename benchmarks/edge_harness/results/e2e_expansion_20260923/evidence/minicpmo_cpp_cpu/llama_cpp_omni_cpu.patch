diff --git a/tools/omni/omni-cli.cpp b/tools/omni/omni-cli.cpp
index 2e9623a..769786d 100644
--- a/tools/omni/omni-cli.cpp
+++ b/tools/omni/omni-cli.cpp
@@ -11,6 +11,7 @@
 
 #include <iostream>
 #include <chrono>
+#include <thread>
 #include <vector>
 #include <limits.h>
 #include <cinttypes>
@@ -61,6 +62,8 @@ static void show_usage(const char * prog_name) {
         "  --ref-audio <path>  Reference audio for voice cloning (default: tools/omni/assets/default_ref_audio/default_ref_audio.wav)\n"
         "  -c, --ctx-size <n>  Context size (default: 4096)\n"
         "  -ngl <n>            Number of GPU layers (default: 99)\n"
+        "  --t2w-device <mode> Token2Wav device: cpu or gpu:<index> (default: gpu:0)\n"
+        "  --t2w-wait-seconds <n> Fail if async audio is incomplete after n seconds (default: 120)\n"
         "  --no-tts            Disable TTS output\n"
         "  --omni              Enable omni mode (audio + vision, media_type=2)\n"
         "  --vision-backend <mode>  Vision compute backend: 'metal' (default) or 'coreml' (ANE)\n"
@@ -209,6 +212,8 @@ int main(int argc, char ** argv) {
     std::string ref_audio_path = "tools/omni/assets/default_ref_audio/default_ref_audio.wav";
     int n_ctx = 4096;
     int n_gpu_layers = 99;  // GPU 层数，默认 99
+    std::string token2wav_device = "gpu:0";
+    int token2wav_wait_seconds = 120;
     int media_type = 1;     // 1=audio only, 2=omni (audio+vision)
     bool use_tts = true;
     bool run_test = false;
@@ -247,6 +252,20 @@ int main(int argc, char ** argv) {
         else if (arg == "-ngl" && i + 1 < argc) {
             n_gpu_layers = std::atoi(argv[++i]);
         }
+        else if (arg == "--t2w-device" && i + 1 < argc) {
+            token2wav_device = argv[++i];
+            if (token2wav_device != "cpu" && token2wav_device.rfind("gpu:", 0) != 0) {
+                fprintf(stderr, "Error: --t2w-device must be cpu or gpu:<index>\n");
+                return 1;
+            }
+        }
+        else if (arg == "--t2w-wait-seconds" && i + 1 < argc) {
+            token2wav_wait_seconds = std::atoi(argv[++i]);
+            if (token2wav_wait_seconds < 1 || token2wav_wait_seconds > 3600) {
+                fprintf(stderr, "Error: --t2w-wait-seconds must be 1..3600\n");
+                return 1;
+            }
+        }
         else if (arg == "--no-tts") {
             use_tts = false;
         }
@@ -339,6 +358,7 @@ int main(int argc, char ** argv) {
     printf("  TTS enabled: %s\n", use_tts ? "yes" : "no");
     printf("  Context size: %d\n", n_ctx);
     printf("  GPU layers: %d\n", n_gpu_layers);
+    printf("  Token2Wav device: %s\n", token2wav_device.c_str());
     printf("  Vision backend: %s\n", vision_backend.c_str());
     if (vision_backend == "coreml") {
         printf("  Vision CoreML: %s\n", vision_coreml_model_path.c_str());
@@ -347,7 +367,8 @@ int main(int argc, char ** argv) {
     printf("  Ref audio: %s\n", ref_audio_path.c_str());
     
     // 🔧 Token2Wav 使用 GPU（Metal），已用 ggml_add+ggml_repeat 替代不支持的 ggml_add1
-    auto ctx_omni = omni_init(&params, media_type, use_tts, tts_bin_dir, -1, "gpu:0");
+    auto ctx_omni = omni_init(&params, media_type, use_tts, tts_bin_dir,
+                              token2wav_device == "cpu" ? 0 : -1, token2wav_device);
     if (ctx_omni == nullptr) {
         fprintf(stderr, "Error: Failed to initialize omni context\n");
         return 1;
@@ -366,13 +387,19 @@ int main(int argc, char ** argv) {
     }
 
     // 等待 T2W 完成所有音频生成后再停止线程
+    bool audio_complete = true;
     if(ctx_omni->async && ctx_omni->use_tts) {
         std::string done_flag = std::string(ctx_omni->base_output_dir) + "/round_000/tts_wav/generation_done.flag";
         fprintf(stderr, "Waiting for audio generation to complete...\n");
-        for (int i = 0; i < 1200; ++i) {  // 最多等 120 秒
+        audio_complete = false;
+        for (int i = 0; i < token2wav_wait_seconds * 10; ++i) {
             FILE * f = fopen(done_flag.c_str(), "r");
-            if (f) { fclose(f); fprintf(stderr, "Audio generation completed.\n"); break; }
-            usleep(100000);  // 100ms
+            if (f) { fclose(f); audio_complete = true; fprintf(stderr, "Audio generation completed.\n"); break; }
+            std::this_thread::sleep_for(std::chrono::milliseconds(100));
+        }
+        if (!audio_complete) {
+            fprintf(stderr, "Error: audio generation did not complete within %d seconds.\n",
+                    token2wav_wait_seconds);
         }
     }
 
@@ -386,5 +413,8 @@ int main(int argc, char ** argv) {
     llama_perf_context_print(ctx_omni->ctx_llama);
 
     omni_free(ctx_omni);
+    if (!audio_complete) {
+        return 2;
+    }
     return 0;
 }
diff --git a/tools/omni/omni.h b/tools/omni/omni.h
index 145f515..b35c459 100644
--- a/tools/omni/omni.h
+++ b/tools/omni/omni.h
@@ -13,7 +13,7 @@
 #include <atomic>
 
 // Windows compatibility: pid_t is not defined on MSVC
-#ifdef _WIN32
+#if defined(_WIN32) && !defined(__MINGW32__)
     typedef int pid_t;
 #endif
 
@@ -485,4 +485,4 @@ void sliding_window_reset(struct omni_context * ctx_omni);
 
 // ==================== 高清模式函数声明 ====================
 // 设置 vision max_slice_nums 覆盖值，用于高清模式
-void vision_set_max_slice_nums(struct vision_ctx * ctx_vision, int max_slice_nums);
\ No newline at end of file
+void vision_set_max_slice_nums(struct vision_ctx * ctx_vision, int max_slice_nums);
