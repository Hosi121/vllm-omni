# Core runtime deps for AR / generation (TTS, speech) pipelines. Media /
# diffusion / video deps live in media.txt; common.txt pulls both.
# transformers: required unified multimodal model classes are unavailable
# before 5.10.1; 5.10.0 is yanked, and 5.15 has a known construction
# regression. Floor is 5.13.0 so transformers.integrations.hub_kernels
# LayerRepository entries always pass version=/revision= required by
# kernels>=0.16 (#6971). Keep in sync with kernels in media.txt.
transformers >= 5.13.0, < 5.15
av>=14.0.0
omegaconf>=2.3.0
safetensors>=0.8.0
accelerate==1.12.0
soundfile>=0.13.1
tqdm>=4.66.0
einops>=0.8.1
prettytable>=3.8.0
aenum==3.1.16
pyzmq>=25.0.0
janus>=1.0.0
msgpack>=1.0.0
pydantic>=2.1.0
pydantic-settings>=2.0.0
